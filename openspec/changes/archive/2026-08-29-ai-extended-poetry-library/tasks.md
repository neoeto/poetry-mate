## 1. 作品模型与本地扩展库

- [x] 1.1 定义扩展作品、AI 寻诗结果和来源可信度模型，支持标题、作者、时期、体裁、正文段落、序文/词牌、出处及未知状态，并实现结构化响应解析与基础字段校验
- [x] 1.2 实现与 `poem-id` 规范一致的正文/序文规范化去重指纹；覆盖繁简、标点、空白和段落边界，并明确不修改公共诗词 ID
- [x] 1.3 新增 `extended_poems` Drift 表、指纹唯一索引及 schema migration，确保已有数据库升级不改写公共诗库、收藏和注本
- [x] 1.4 实现扩展作品仓库及 Riverpod provider，提供按 ID 查询、按指纹查询、保存和按创建时间列表能力
- [x] 1.5 增加公共库与扩展库的统一去重/作品解析入口；公共库优先返回已有作品，公共库后续导入时不重复展示扩展库中的同指纹作品
- [x] 1.6 为模型解析、指纹去重、扩展表迁移、仓库 CRUD 和跨库重复场景补充单元测试

## 2. AI 寻诗会话

- [x] 2.1 实现独立的 AI 寻诗服务：维护当前会话消息，要求模型返回既有作品而非原创/仿写/续写，并支持 `found`/`not_found` 结果、流式响应、格式重试和产品化错误
- [x] 2.2 将寻诗服务接入用户当前人格和 BYOK LLM 客户端，确保会话、候选作品和失败原文不自动写入注本或其他持久化存储
- [x] 2.3 实现 AI 寻诗页面与多轮对话 UI，提供加载、未找到、无 Key、网络/格式错误和重试状态，并展示结构化作品预览及来源可信度提示
- [x] 2.4 在结果页实现“加入扩展诗词库”和“仅本次查看”两个明确动作；保存前执行跨库去重，重复作品提供打开已有作品/提示，不创建副本
- [x] 2.5 为寻诗 prompt、禁止原创、结构化解析、无 Key 引导、未找到结果、保存去重和错误降级补充服务层及 widget 测试

## 3. 阅读页与扩展诗词库接入

- [x] 3.1 增加扩展诗词库独立页面、入口和路由，支持空态、列表、来源状态展示，以及打开已保存作品
- [x] 3.2 为外部作品建立到现有 `Poem` 阅读模型的适配，支持现代诗、《诗经》、古文等可扩展体裁，并在阅读页展示“AI 补充作品/信息待核”提示和安全的未知元数据
- [x] 3.3 增加临时作品阅读上下文和临时路由，使未保存作品可以复用 `ReaderPage` 而不依赖公共/扩展库的持久化查询
- [x] 3.4 扩展 `AnnotationService` 的阅读上下文：持久化模式沿用注本缓存和编辑保护，临时模式仅使用会话内存缓存，L1/L2/L3 及选词解释均禁止读写持久化注本
- [x] 3.5 在 L1、L2、选词解释和追问 UI 中区分持久化/临时模式；临时结果可查看和会话内复用，但不显示注本编辑/删除入口，离开页面取消流并释放临时数据
- [x] 3.6 更新注本列表和作品解析逻辑，使已保存扩展作品的注本可正常聚合展示；确保一次性作品的任何解析不会出现在注本中
- [x] 3.7 为扩展阅读、来源提示、临时解析不落库、持久化扩展作品解析和公共/扩展路由补充 widget/integration 测试

## 4. 验证与收尾

- [x] 4.1 运行 Drift 代码生成、Dart formatter、静态分析和完整 Flutter 测试，修复迁移、类型和现有回归问题
- [x] 4.2 验证公共诗库、收藏、已有注本、BYOK 配置和无 Key 阅读流程不受影响
- [x] 4.3 更新用户可见入口文案和必要的项目文档，明确 AI 寻诗不是原创作诗、外部作品来源可能待核，且所有扩展数据仅保存在本机
